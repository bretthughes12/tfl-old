<% title "Editing Player" %>

<h1><%= yield(:title) %></h1>

<% form_for(@player) do |f| %>
  <%= render :partial => 'form' %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @player %> |
<%= link_to 'Back', players_path %>