<% title "New Player" %>

<h1><%= yield(:title) %></h1>

<% form_for(@player) do |f| %>
  <%= render :partial => 'form' %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', players_path %>